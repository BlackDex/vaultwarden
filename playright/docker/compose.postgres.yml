---
volumes:
  playwright_postgres_data:

services:
  postgres:
    container_name: playwright_postgres
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: testdb
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U testuser -d testdb"]
      interval: 5s
      timeout: 5s
      retries: 15
    volumes:
      - playwright_postgres_data:/var/lib/postgresql/data

  vaultwarden:
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: "postgres://testuser:testpass@playwright_postgres:5432/testdb"

  playwright:
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - playwright_postgres_data:/dbdata
    environment:
      DB_TYPE: postgres
